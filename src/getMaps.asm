; getMaps.asm is a file
; which contains everything
; needed to get maps that
; are pre-generated (from
; the source code) or
; user-made.

poggers:
    mov bx, po
    call printf

po:
    db '1lmfao', 0